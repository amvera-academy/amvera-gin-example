# Gin на Amvera

Простой пример деплоя Gin в [Amvera](https://amvera.ru).

Это тестовое приложение показывает работу API, статических файлов и персистентное сохранение JSON в постоянное хранилище Amvera.

[КАК СОХРАНЯТЬ ФАЙЛЫ](#как-правильно-сохранять-файлы) | [СТАТИЧЕСКИЕ ФАЙЛЫ](#статические-файлы) | [СБОРКА GO](#сборка-go) | [КАК ЗАПУСТИТЬ НА AMVERA](#деплой-в-amvera)

## Демо-приложение

Приложение имеет веб-интерфейс, на котором вы сразу можете выполнить доступные тестовые запросы.

- `GET /api/health`
- `GET /api/items`
- `POST /api/items`
- `DELETE /api/items/{id}`

## Как правильно сохранять файлы

Изменяемые файлы необходимо сохранять в `/data`. Относительный путь вроде `data/items.json` считается от текущей рабочей директории и не является постоянным хранилищем Amvera.

В примере запись защищена `sync.Mutex`, чтобы запросы одного процесса не перезаписали файл одновременно. Для нескольких экземпляров приложения используйте общую базу данных.

## Статические файлы

Файлы находятся в `static` и подключаются отдельными маршрутами Gin:

```go
router.StaticFile("/", "static/index.html")
router.StaticFile("/app.js", "static/app.js")
router.StaticFile("/styles.css", "static/styles.css")
```

Пути чувствительны к регистру. Рабочая директория при запуске должна содержать папку `static` рядом с бинарным файлом.

## Сборка Go

`go build -o app .` создает один бинарный файл, но директорию `static` все равно необходимо загрузить вместе с ним. Приложение слушает `0.0.0.0:5000`.

Файлы `go.mod` и `go.sum` необходимо хранить в репозитории. После добавления или обновления зависимостей выполните локально `go mod tidy` и загрузите оба измененных файла. Во время сборки приложение использует уже зафиксированные версии и контрольные суммы.

Для production можно задать `GIN_MODE=release`, чтобы отключить отладочные сообщения Gin.

## Локальный запуск

```bash
go mod download
go run .
```

Откройте localhost:5000.

## Деплой в Amvera

Для деплоя конкретно этого приложения вам понадобится:

1. Создать аккаунт в [Amvera](https://cloud.amvera.ru);
2. Создать обычное приложение в любом регионе;
3. Загрузить в него код репозитория;
4. Во вкладке "Конфигурация" нажать кнопку "Собрать".

Когда приложение будет готово к работе и статус сменится на "Запущено", во вкладке "Домены" можно будет создать бесплатное доменное имя от Амвера.
